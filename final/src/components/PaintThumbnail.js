import React from 'react';

const PaintThumbnail = () => {
    return (
        <div style={ styles }>
            Thumbnail 
        </div>
    )
}

const styles = {
    height: '50%',
    width: '25%',
    background: 'red',
}

export default PaintThumbnail